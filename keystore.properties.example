# Copy this file to `keystore.properties` (which is gitignored) and fill in real values.
# Then `./gradlew :app:assembleRelease` produces a SIGNED release APK.
# Absolute path to your release keystore (keep it OUTSIDE the repo and BACK IT UP):
storeFile=C:/Mohsin/keys/marifatul-release.keystore
storePassword=CHANGE_ME
keyAlias=marifatul
keyPassword=CHANGE_ME
