# ASR environments — large, machine-specific, recreatable
.venv-asr/     # scripts/match-audio.sh, scripts/align-ayat.sh (faster-whisper)
.venv-align/   # scripts/find-onsets.sh (torch + the MMS aligner)

# Cached Whisper transcripts (scripts/match-audio.py)
.cache/

# Byproduct of importing scripts/match-audio.py from scripts/align-ayat.py
__pycache__/
